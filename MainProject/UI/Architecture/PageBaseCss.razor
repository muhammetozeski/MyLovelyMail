@namespace MyLovelyMail.MainProject.UI.Layout.Architecture
@inherits MyLovelyMail.MainProject.UI.Layout.Architecture.ThemedComponentBase

<style>
    .page-error-container {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: @(AppMeasures.Viewport.Full)vh;
        padding: @(AppMeasures.Space.Large)px;
    }

    .page-error-card {
        max-width: @(AppMeasures.Size.Px420)px;
        width: @(AppMeasures.Percent.Full)%;
        text-align: center;
    }

    .page-error-icon {
        font-size: @(AppMeasures.Font.Hero)px;
        margin-bottom: @(AppMeasures.Space.Medium)px;
    }
</style>

