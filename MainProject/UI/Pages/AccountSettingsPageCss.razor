@namespace MyLovelyMail.MainProject.UI.Pages
@inherits ThemedComponentBase

<style>
    @* The badge, control-strip and input rules moved to SettingEditorRowCss, which ships with the
       component that uses them instead of living on one page that happened to be its only host. *@
    .settings-number--wide {
        width: @(AppMeasures.Size.Px220)px;
    }

    .settings-password-row {
        display: flex;
        align-items: center;
        gap: @(AppMeasures.Space.Px8)px;
    }
</style>
