# Account rail you can order, and a paused account that looks paused

Kategori: Account management | Efor: M | Rank: 1

## Ne
MailAccountData.cs gets an `int SortOrder`. AccountStore.Load replaces `accounts.Sort(static (a, b) => a.CreatedUtc.CompareTo(b.CreatedUtc))` (line 48) with a sort on (SortOrder, CreatedUtc), back-filling SortOrder from the existing CreatedUtc order the first time an account.json has none so nothing jumps for current accounts. A new `AccountStore.Move(string accountId, int delta)` swaps neighbours, renumbers every account, writes each account.json through the existing Save path, and raises OnAccountsChanged. Settings.razor's Accounts card (the `@foreach (var account in AccountStore.Accounts)` block at line 274) gains a small up/down pair per row. In Mail.razor's rail (lines 49-68) a `!account.Enabled` account renders with a `mail-account--paused` class alongside the existing `.mail-account` / `.mail-account-dot` / `.mail-account-ordinal` styles in MailCss.razor (lines 81/101/115) — desaturated dot, a pause glyph next to the existing `.mail-account-failing` warning glyph, tooltip "Paused - not syncing". It stays clickable and keeps its Ctrl+1..9 ordinal so cached mail is still readable. DebugApi.cs gets `POST /accounts/order?accountId=&delta=`, and the `accounts` array in `GET /status` gains SortOrder. SCOPE TRIMMED FROM THE PROPOSAL: drop the inline Enabled CoreToggle in Settings.razor — AccountSettingsPage.razor line 54 already has exactly that control bound to SetEnabled, so it is duplicate surface across two pages rather than a fix. The rail rendering is what makes a paused account legible; keep the change to ordering plus that.

## Neden
Verified against the repo: no `SortOrder` exists anywhere in the solution, and Mail.razor.Interaction.cs lines 230-233 do `SelectAccount(AccountStore.Accounts[accountIndex])`, indexing straight into the CreatedUtc-sorted list — so which mailbox answers Ctrl+1 is decided by signup date and cannot be changed. Separately, `Enabled` is honoured by SyncScheduler.cs:83, ImapIdleService.cs:33 and OutboxService.cs:149, but the rail block draws a disabled account identically to a live one, so a mailbox that stopped syncing is indistinguishable from one that is up to date — the flag has real consequences and zero visual trace. Both halves live in the same rail and are mechanically simple: a comparer change with a back-fill, a neighbour swap, and one CSS class. Fully verifiable through the DebugApi without external services: POST /accounts/order, read GET /status for the new order and SortOrder values, then GET /dom?selector=.mail-sidebar to confirm the rail and its ordinals followed; disable an account through the existing account settings path and re-read /dom for the mail-account--paused class.
