# Reader text size stepper (and a document that knows its own direction)

Kategori: Reader typography | Efor: S | Rank: 1

## Ne
Add `Settings.ReaderTextScalePercent` (default 100, clamp 70-200) beside `FoldQuotedText` in `MainProject\Stores\Settings.cs:94`. In `MailBodyRenderer.WrapDocument` (`MainProject\Services\Mail\MailBodyRenderer.cs:179-188`) replace the hardcoded `font-size:14px` with `font-size:{14 * scale / 100}px` and turn the quote pill's `font-size:12px` into `0.85em` so it tracks. No signature change is needed: `Render` already reads `Settings.FoldQuotedText.Value` and `Settings.ExternalImages.Value` inline, so reading the new setting the same way means `GET /message` in `DebugApi.cs:412` returns the scaled document for free. Surface an `A- 100% A+` group in the `mail-reader-actions` strip (`MainProject\UI\Pages\Mail.razor:499-510`, same `SecondaryButton` shape as Reply/Forward) that writes the setting and re-renders via the exact `loadedBodyUid = 0; _ = LoadOpenBodyIfNeededAsync();` path `AllowImagesOnce` uses (`Mail.razor.Reader.cs:133-139`), plus `Ctrl+=`/`Ctrl+-`/`Ctrl+0` cases in the `HandleKey` switch (`Mail.razor.Interaction.cs:203+`) and a row in `ShortcutHelpRows` (:18). Verify with `curl '127.0.0.1:52539/message?accountId=..&uid=..'` and assert the new px value in `bodyHtml`; add `POST /reader-scale` to `DebugApi.cs` so the setting is drivable headlessly. Ship the bidi change as a SEPARATE second commit in the same method — `dir="auto"` on `<body>` and `unicode-bidi:plaintext` on `pre`/`blockquote` — and state plainly that the curl proves only that the attribute is emitted, not that an Arabic message lays out correctly.

## Neden
The 14px inside the sanitized iframe is the one piece of type the user cannot change: `UiScalePercent` scales the entire shell and `MessageListDensity` only affects list rows, so making mail readable today means resizing the whole app. Confined to one string builder plus one settings field, and because `Render` already pulls settings internally the verification is a single curl against an endpoint that already returns `bodyHtml`.
