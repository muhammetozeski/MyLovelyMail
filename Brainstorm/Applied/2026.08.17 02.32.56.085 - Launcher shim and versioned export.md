# Launcher shim and versioned export

Kategori: reporting | Efor: M | Rank: 10

## Ne
Adapt CreateLauncher.ps1 pattern: build tiny launcher exe placed at MyLovelyMail root that starts AppData\MyLovelyMail.exe forwarding args. Export.ps1 publishes win-x64 into C:\E\kp\aaBenimProgramlarim\MyLovelyMail\v<N>\ (auto-increment folder) laying out AppData + launcher; app creates UserData/UserCache/AppCache at first run.

## Neden
User-specified deployment layout and per-loop versioned export to avoid file locks.

