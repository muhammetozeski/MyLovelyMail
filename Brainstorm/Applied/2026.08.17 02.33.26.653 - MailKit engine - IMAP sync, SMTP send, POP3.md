# MailKit engine - IMAP sync, SMTP send, POP3

Kategori: resilience | Efor: L | Rank: 4

## Ne
Add MailKit + Polly packages. ImapSyncService (folder list, incremental fetch by UID, idle push), SmtpSendService, Pop3Service. All network calls wrapped in central ResiliencePolicy (retry + timeout + circuit breaker) per KodlamaPrensipleri rule 43.

## Neden
The core of a mail client; Polly hardening was demanded every loop.
