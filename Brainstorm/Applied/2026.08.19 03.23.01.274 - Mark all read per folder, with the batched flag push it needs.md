# Mark all read per folder, with the batched flag push it needs

Kategori: Triage | Efor: S | Rank: 2

## Ne
Add `MessageActions.SetFolderRead(MailAccountData account, string folderFullName)` in MainProject/Services/Mail/MessageActions.cs: take the unread summaries from `MessageStore.GetSummaries(account.Id, folderFullName)`, OR in `MailFlags.Seen` on each, push all of them through ONE `MessageStore.UpsertSummaries(account.Id, folderFullName, changed)` call (it takes an IEnumerable and does SaveIndex + RefreshUnreadCount + OnFolderChanged once per call), then one batched server push through the existing private `RunServerActionInBackground` helper with `folder.AddFlagsAsync([.. uids.Select(u => new UniqueId(u))], MessageFlags.Seen, silent: true, ct)` — the same batching shape `MoveToFolder` already uses with `MoveToAsync([...], target, ct)`. Return early when nothing is unread. Then point `BulkSetRead(true)` in Mail.razor.Interaction.cs:89 at the batched path when the checked set covers the whole visible folder, so the same sitting removes the per-message connection storm: today each `SetRead` rewrites index.jsonl AND opens its own ImapClient inside `RunServerActionInBackground`, so a 400-unread folder means 400 index rewrites and 400 IMAP connections. UI: a second sibling button inside the `mail-folder-row` wrapper in Mail.razor (line 88-94), next to the existing `mail-folder-resync` 🔄 button, rendered only when `folder.UnreadCount > 0`, title `Mark all N as read`. No new key binding. Verification is headless through the DebugApi: add a `POST /folder/read-all?accountId=&folder=` case to MainProject/zTests/DebugApi.cs, then assert `GET /folders` reports `unreadCount: 0` for that folder, `GET /list?query=is:unread` comes back empty, and `GET /logs` shows a single batched push line instead of one per message.

## Neden
Every unread number in the app — the folder chip, the per-account badge from `AccountUnreadCount`, the tray badge — points at mail the user has already given up on, and the only way to clear a noisy folder today is checking rows by hand in the bulk bar. A grep for MarkAllRead/SetFolderRead across the tree finds nothing, so it is genuinely absent, and it is the most-reached-for action in a daily-driver mail client. It also pays for itself by fixing a live scaling problem in the existing bulk path rather than adding a parallel one. Snoozed rows are already marked Seen by SnoozeService, so they stay out of the unread set and cannot be woken by accident.
