# Tray icon, autostart, background mode

Kategori: ux | Efor: M | Rank: 9

## Ne
H.NotifyIcon.Maui tray icon with context menu (open, compose, sync now, quiet mode, exit). Close-to-tray setting, start-minimized setting, run-at-Windows-startup setting (registry Run key). New-mail toast notifications with per-filter sound.

## Neden
Explicitly requested: startup launch, tray minimize, notification system.

