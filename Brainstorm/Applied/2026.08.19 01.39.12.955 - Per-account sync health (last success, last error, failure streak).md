# Per-account sync health (last success, last error, failure streak)

Kategori: operational-visibility | Efor: M | Rank: 1

## Ne
New MainProject/Services/Mail/SyncHealthService.cs holding a record per account id: LastAttemptUtc, LastSuccessUtc, LastErrorMessage, LastErrorUtc, ConsecutiveFailures. Persist ALL accounts in one UserData/sync-health.json (dictionary keyed by account id) through the existing AtomicFile + JsonDefaults path - do NOT add a public AccountCachePath to MessageStore; this state is not mail cache and needs no lower-layer change at all. Wire it in exactly one place: the per-account try/catch inside SyncScheduler.SyncNowAsync (SyncScheduler.cs:83-100) - MarkAttempt before the Imap/Pop3 branch, MarkSuccess after it, MarkFailure(ex) in the catch at line 96 that today only calls Log(). Add a second MarkFailure in the ImapIdleService reconnect catch (ImapIdleService.cs:123) so a dropped push connection shows too, and raise OnHealthChanged (same shape as SyncScheduler.OnSyncStateChanged) so the UI repaints. Surface it twice: the sidebar account button in Mail.razor:50-61 gets a small status dot beside .mail-account-dot plus a title tooltip ("Last synced 14:32" / "Failed 3x - Authentication failed"), and the Accounts row in Settings.razor:243 swaps its hint line (@account.Protocol - @account.IncomingHost) for the last-sync time or the error text. New CSS class in MailCss.razor beside .mail-folder-resync (line 148). DebugApi gets GET /health and the same fields folded into the accounts projection of GET /status.

## Neden
Verified: no health state exists anywhere in the repo, and SyncScheduler.cs:96-99 catches a failing account with nothing but one Log() line that the next pass overwrites. Both sync engines let ResiliencePolicy.RunNetwork throw, so that catch really is the single chokepoint every account failure passes through. Today a mailbox whose password expired looks exactly like a quiet one, and the only sync signal in the app (SyncHeart's lastSyncEnd) is global, in-memory and dies with the component. End-to-end verifiable without the screen: POST /accounts with a bad host, POST /sync, GET /health shows ConsecutiveFailures climbing and the error text; fix the host, sync again, LastSuccessUtc moves and the streak resets.
