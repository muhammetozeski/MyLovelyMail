# Unsubscribe chip in the reader header

Kategori: Reader pane | Efor: S | Rank: 1

## Ne
New `MainProject/Services/Mail/UnsubscribeService.cs`, a pure consumer of `MessageStore.TryLoadMimeMessage(accountId, folder, uid)` in the same shape as `AttachmentService.List`: parse the `List-Unsubscribe` header into `record UnsubscribeTargets(string? HttpUrl, string? MailtoAddress, string? MailtoSubject)`, splitting the comma-separated `<...>` forms and keeping the http(s) and mailto: variants apart; return null when the header is absent. In `MainProject/UI/Pages/Mail.razor.Reader.cs` add `UnsubscribeTargets? unsubscribeTargets` plus `uint unsubscribeUid`, using the same uid-keyed memo guard as `senderHistory`/`senderHistoryUid` (the reader re-renders on every state change), and fill it in `LoadOpenBodyIfNeededAsync` right where `OpenAttachments` is assigned (line 117). In `Mail.razor` render one chip under the existing `.mail-reader-history` button (line 359): the mailto form builds a prefilled draft via `ComposeService.BuildNew(account)` + `MailUiState.OpenCompose(draft)` so the user still presses Send; the http form opens the default browser with `Process.Start(new ProcessStartInfo(url){UseShellExecute=true})` in try/catch reporting into the existing `SaveStatus` line — MainProject is a plain net10.0 Razor library, not a MAUI-SDK project, so `Launcher.OpenAsync` is not available and `Process.Start` is the right call. Style `.mail-unsubscribe-chip` in `MailCss.razor` cloned from the `.mail-reader-history` block (`AppMeasures.Radius.Pill`, `AppColors.TextSecondary`). No RFC 8058 one-click POST — nothing fires without an explicit click, and never auto-open the URL on message open. Verify with a new `GET /unsubscribe` case in `MainProject/zTests/DebugApi.cs` alongside the existing `/source` handler: accept `path=<fixture.eml>` over three hand-written fixtures (http only, mailto only, both, plus one with no header asserting null), then `POST /open` a real newsletter and read the parsed record back through `accountId&folder&uid` the way `/message` does.

## Neden
Newsletters are the bulk of a daily driver's inbox and today the unsubscribe link is unreachable: the reader iframe is `sandbox="allow-same-origin"` with no `allow-popups` (Mail.razor:453), and grepping the repo for `Process.Start`/`Launcher.` returns zero hits, so the app has no path to the default browser at all. `List-Unsubscribe` has zero hits in the codebase, so nothing here is duplicated. The header is already sitting in the cached .eml that `OpenMessageSource` walks for the Source modal, so no new fetch, no new storage, no network. It also composes with the shipped rule engine: an unsubscribe chip plus a delete-from-this-sender rule is the whole newsletter-triage loop.
