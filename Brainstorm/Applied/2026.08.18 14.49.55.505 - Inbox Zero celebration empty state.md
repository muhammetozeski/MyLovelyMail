# Inbox Zero celebration empty state

Kategori: delight-animation | Efor: S | Rank: 2

## Ne
Upgrade the plain 🕊️ "This folder is empty" state into a celebration, but only when the empty folder is the Inbox: in Mail.razor's `FilteredSummaries.Count == 0` branch (the non-syncing else at lines 118-124), branch on `MailUiState.SelectedFolder is { Role: FolderRole.Inbox }` and blank SearchText to render a `mail-inbox-zero` state — a gradient "Inbox Zero 💖" headline plus 4-5 tiny CSS-only hearts drifting upward on loop. In MailCss.razor add the `inbox-zero-drift` keyframes beside the existing `mail-bloom` block, reusing the theme's accent CSS variables so account accent colors tint the hearts. Skip the optional JustReachedZero burst flag — the steady celebration state is the deliverable; the one-shot burst can be its own later change.

## Neden
Emptying the inbox is the payoff moment of the already-shipped keyboard-triage and bulk-bar features, and today it gets the same generic dove as any empty folder (verified at Mail.razor lines 120-123). Pure UI layer (Mail.razor + MailCss.razor), fits the pastel identity, and is the more verifiable of the two proposals: empty the inbox through the DebugApi and the celebration is a steady rendered state, not a timing-sensitive transient overlay. The competing paper-plane idea rested on a false premise (send success already plays a sound at OutboxService.cs line 88) and would celebrate before the send outcome is known.
