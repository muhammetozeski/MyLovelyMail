# Tag a whole selection at once, and a 't' key on the focused row

Kategori: triage-speed | Efor: S | Rank: 2

## Ne
Add MessageActions.AddTagToMany(account, folderFullName, summaries, tagName) / RemoveTagFromMany next to SetManyRead in MainProject/Services/Mail/MessageActions.cs, built on the same shape: group by folder, mutate summary.Tags on each stored instance, and issue ONE MessageStore.UpsertSummaries per folder instead of the per-message write ToggleTag (MessageActions.cs:64-72) does today. Rows come from MessageStore.GetSummaries, which returns index.Summaries.Values, so the summaries are the stored instances and the !ReferenceEquals guard at MessageStore.cs:191 skips CarryOverLocalState — which is what makes removing the last tag stick instead of being restored by the incoming.Tags.Count == 0 carry-over at MessageStore.cs:211. No server push: tags never leave the machine. Wire it into the bulk bar at Mail.razor:204-252, which today has Read/Unread/Star/Important/Delete/Move/Snooze and no Tag: a 'Tag' dropdown in the same <span class="mail-bulk-move"> shape the Move and Snooze menus use, listing TagStore.AllTags chips plus a new-tag input that calls TagStore.ColorOf first (the registration line at Mail.razor.Reader.cs:233). Give it its own bound field — AddNewTag(message) at Mail.razor.Reader.cs:229 is tied to the open message and to the single NewTagName, so the reader input cannot simply be shared. Applies to SelectedSummaries (Mail.razor.Interaction.cs:83) and clears the selection like BulkMoveTo does. Then add case "t" or "T" when focusedFolder != null to the key switch at Mail.razor.Interaction.cs:226-290, opening the same menu against MailUiState.FocusedMessage, closed by the existing Escape arm at line 264, and add the row to ShortcutHelpRows at Mail.razor.Interaction.cs:18-38. Verify with a new DebugApi case ("POST", "/tag") taking folder, a uid list and the tag name (plus a remove flag) calling AddTagToMany/RemoveTagFromMany directly, then GET /list?query=tag:X for the batch and GET /messages to confirm the single write left Seen/Flagged/snooze intact.

## Neden
Tagging is the only local triage action with no bulk path and no key — read, star, important, delete, move and snooze all work on a selection, and u/s/i/m/x all have keys, but filing 40 receipts means opening 40 messages and clicking the reader picker 40 times. It also avoids the per-message index rewrite a batch would otherwise cost, which is the exact cost SetManyRead's own comment (MessageActions.cs:30-36) was written to remove for read state. Small, entirely local, and every step verifiable through the DebugApi.
